import Foundation

/// Template — copy this file to Secrets.swift (gitignored, not this file)
/// and fill in the real backend API key. See docs/plan.md's "Backend
/// Deployment" section.
enum Secrets {
    static let backendAPIKey = "REPLACE_ME"
}
